# AI Provider API Keys (add your own — stored server-side, never sent to browser)
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
GOOGLE_API_KEY=AIza...

# Path to relacon-wm-mcp index.js (relative to this project root, or absolute)
MCP_SERVER_PATH=../relacon-wm-mcp/index.js

# Server
PORT=3001

# Storage backend: local (JSON files) | supabase (Phase 2)
STORAGE_BACKEND=local
